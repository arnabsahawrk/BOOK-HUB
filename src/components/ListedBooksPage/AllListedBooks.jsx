import React from "react";
import PropTypes from "prop-types";

const AllListedBooks = ({ props }) => {
  return <div>{props}</div>;
};

AllListedBooks.propTypes = {};

export default AllListedBooks;
